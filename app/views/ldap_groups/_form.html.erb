<%= form_for(@ldap_group, html: {class: 'form-horizontal', role: 'form'}) do |f| %>
  <% if @ldap_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ldap_group.errors.count, "error") %> prohibited this ldap_group from being saved:</h2>

      <ul>
      <% @ldap_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= render 'attribute_fields', f: f %>

    <div class = "actions">
      <%= f.submit 'Save Group', class: 'btn btn-primary active' %>
    </div>
<% end %>
