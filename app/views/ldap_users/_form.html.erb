<%= form_for(@ldap_user, html: {class: 'form-horizontal',
                                role: 'form',
                                'ng-controller' => 'FormTest',
                                'ng-submit' => 'submitForm()',
                                'ng-init' => "FormMethod('#{@action}')"}) do |f| %>
    <% if @ldap_user.errors.any? %>
        <div id = "error_explanation">
          <h2><%= pluralize(@ldap_user.errors.count, 'error') %> prohibited this ldap_user from being saved:</h2>

          <ul>
            <% @ldap_user.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <div class = "actions">
      <%= f.submit 'Save User', class: 'btn btn-primary active' %>
      <span ng-click="submitForm()" class="btn btn-danger btn-sm active">Submit HERE</span>
    </div>
    <%= render 'attribute_fields_test', f: f %>
<% end %>
